{
  "type": "module",
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs && node scripts/rtlcss.mjs",
    "docs:preview": "vitepress preview docs",
    "deploy": "pnpm run docs:build && wrangler deploy",
    "preview": "pnpm run docs:build && wrangler dev"
  },
  "devDependencies": {
    "postcss": "^8.5.15",
    "postcss-rtlcss": "^6.0.0",
    "vitepress": "^2.0.0-alpha.17",
    "wrangler": "^4.98.0"
  },
  "dependencies": {
    "@vite-pwa/vitepress": "^1.1.0",
    "vitepress-plugin-lightbox": "^1.0.3",
    "vitepress-plugin-tabs": "^0.9.0"
  }
}
